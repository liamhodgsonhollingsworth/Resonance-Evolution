"""
Apeiron renderer-node modules. Like node_types/, each .py file is one
node-type — the distinction is that renderers' emit() implementations
provide non-default compositing (text rendering, ASCII rendering, etc.).
Picked up by the engine's discovery walk in the same way.
"""
